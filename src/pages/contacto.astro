---
import Layout from '../layouts/Layout.astro';
import Nav from '../components/Nav.astro';
import ContactForm from '../components/ContactForm.astro';
---

<Layout title="Contacto | codev">
	<Nav />
	
	<main class="pt-40 pb-20 px-4 bg-[#031605] min-h-screen flex items-center">
		<div class="max-w-7xl mx-auto grid lg:grid-cols-2 gap-16 xl:gap-32 items-center">
      <div class="space-y-12 contact-info opacity-0 -translate-x-20">
        <div>
          <span class="text-lime text-sm font-bold tracking-[0.2em] uppercase block mb-4">Contacto</span>
          <h1 class="text-6xl md:text-8xl font-light uppercase leading-none tracking-tighter">
            Hagámoslo <br/><span class="text-[rgba(224,204,250,1)] italic font-light">realidad.</span>
          </h1>
        </div>
        
        <p class="text-white/60 text-lg md:text-xl max-w-md leading-relaxed font-light">
          ¿Tienes una idea innovadora? Estoy aquí para escucharla y empezar a construir juntos.
        </p>
      </div>

      <div class="contact-form-container opacity-0 translate-x-20">
        <ContactForm />
      </div>
    </div>
	</main>

  <script>
    import { gsap } from 'gsap';

    document.addEventListener('DOMContentLoaded', () => {
      gsap.to('.contact-info', {
        opacity: 1,
        x: 0,
        duration: 1.2,
        ease: 'power4.out'
      });

      gsap.to('.contact-form-container', {
        opacity: 1,
        x: 0,
        duration: 1.2,
        delay: 0.2,
        ease: 'power4.out'
      });
    });
  </script>
</Layout>
